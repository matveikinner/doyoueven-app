export default class ExerciseInteractor {
  // Inject Network client to add exercise to backend where backend logic will handle
  // with repository pattern adding data first to local database, and then making API call
}
